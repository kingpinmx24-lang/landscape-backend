services:
  - type: web
    name: landscape-backend
    runtime: node
    buildCommand: pnpm install --prod --ignore-scripts
    startCommand: node dist/index.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
